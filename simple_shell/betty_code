#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: This is a program that passes Betty checks
 * It prints "Hello, Betty!" to the standard output and ends with a new line
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	printf("Hello, Betty!\n");
	return (0);
}
